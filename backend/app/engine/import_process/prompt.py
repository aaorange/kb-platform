# -*- coding: utf-8 -*-
"""导入图提示词 — 实体泛化：商品 → 文档主体"""

IMAGE_SUMMARY = """这是"{file_title}"文件中的一张图片，图片上文部分为"{context[0]}",
下文部分为"{context[1]}"，请用中文简要总结这张图片的内容，用于markdown图片标题
"""

NAME_RECOGNITION = """请从以下信息中识别出这份文档所属的"主体名称"。

文档主体可能是：产品/设备型号、制度/规范名称、项目/系统代号、业务领域名称等。
文件名：{file_title}

正文切片（用于辅助识别）：
{context}

要求：
1. 返回内容为字符串形式，尽量带品牌、型号和名称的完整主体名称。比如：苏泊尔5000W大功率电磁炉、差旅费报销管理办法、XX项目验收规范；
2. 返回结果只包含主体名称本身，不要添加任何解释或其他内容；
3. 如果无法识别品牌/型号，则无需返回品牌/型号，仅返回名称；
4. 如果完全无法识别主体名称，请返回文件标题本身。
"""
